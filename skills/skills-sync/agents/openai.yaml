interface:
  display_name: "Skills Sync"
  short_description: "Publish local skill changes upstream as a PR"
  default_prompt: "Use $skills-sync to push locally edited or new skills back to the shared skills repo."
allow_implicit_invocation: false
