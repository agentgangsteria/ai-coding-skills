interface:
  display_name: "Commit Conventions"
  short_description: "Commit message + branch-name conventions: append-only history behind a propose-then-approve gate that reads the finished diff before every branch and every commit."
  default_prompt: "Use $commit-conventions when creating a commit, naming a branch, or rewriting history."
